# Copy to .env and fill in your token.
# Classic PAT — required scopes: `notifications` + `repo` (or `public_repo` for public repos only).
# Create one at https://github.com/settings/tokens/new
export GITHUB_TOKEN="ghp_REPLACE_ME"
